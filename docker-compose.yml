services:
  cc-proxy:
    build: .
    ports:
      - "18081:18081"
    volumes:
      - ./config.yaml:/app/config.yaml:ro
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
